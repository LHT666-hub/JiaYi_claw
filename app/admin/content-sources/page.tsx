"use client";

import { FormEvent, useCallback, useEffect, useState } from "react";
import Link from "next/link";
import { ArrowLeft, ExternalLink } from "lucide-react";
import { useToast } from "@/components/ToastProvider";

export default function ContentSourcesPage() { const { showToast } = useToast(); const [sources, setSources] = useState<Array<Record<string, unknown>>>([]); const [candidateCount, setCandidateCount] = useState(0); const [source, setSource] = useState({ name: "", sourceType: "wechat_article", sourceUrl: "" }); const [ingest, setIngest] = useState({ sourceId: "", url: "", category: "activity" });
  const load = useCallback(async () => { const response = await fetch("/api/v1/admin/content-sources", { cache: "no-store" }); const payload = await response.json(); if (response.ok) { setSources(payload.data.sources ?? []); setCandidateCount(payload.data.candidates?.length ?? 0); } }, []); useEffect(() => { void load(); }, [load]);
  async function add(event: FormEvent) { event.preventDefault(); const response = await fetch("/api/v1/admin/content-sources", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(source) }); const payload = await response.json(); if (!response.ok) return showToast(payload.error?.message ?? "添加失败", "warning"); showToast("官方内容来源已登记。", "success"); await load(); }
  async function run(event: FormEvent) { event.preventDefault(); const response = await fetch("/api/v1/admin/content-sources/ingest", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(ingest) }); const payload = await response.json(); if (!response.ok) return showToast(payload.error?.message ?? "采集失败", "warning"); showToast("候选内容已生成，等待人工审核。", "success"); setCandidateCount((value) => value + 1); }
  const input = "h-11 w-full rounded-md border border-line px-3 text-sm outline-none focus:border-sage";
  return <main className="min-h-dvh bg-[#F3F5F4] text-navy"><header className="border-b border-line bg-white"><div className="mx-auto flex max-w-6xl items-center gap-3 px-5 py-4"><Link href="/admin" className="flex h-10 w-10 items-center justify-center rounded-md border border-line"><ArrowLeft className="h-4 w-4" /></Link><div><h1 className="text-xl font-semibold">官方内容来源</h1><p className="mt-1 text-xs text-navy/50">只采集摘要与原文入口，全部候选内容人工审核</p></div></div></header><div className="mx-auto max-w-6xl px-5 py-6"><div className="grid gap-4 lg:grid-cols-2"><form onSubmit={add} className="rounded-md border border-line bg-white p-5"><h2 className="font-semibold">登记官方来源</h2><div className="mt-4 grid gap-3"><input required value={source.name} onChange={(e) => setSource({ ...source, name: e.target.value })} placeholder="来源名称，例如机构公众号" className={input} /><select value={source.sourceType} onChange={(e) => setSource({ ...source, sourceType: e.target.value })} className={input}><option value="wechat_article">微信公众号文章</option><option value="official_website">官方网站</option><option value="rss">RSS</option><option value="open_api">开放接口</option><option value="manual">人工维护</option></select><input required type="url" value={source.sourceUrl} onChange={(e) => setSource({ ...source, sourceUrl: e.target.value })} placeholder="HTTPS 官方主页或文章地址" className={input} /><button className="rounded-md bg-navy px-4 py-3 text-sm font-semibold text-white">登记来源</button></div></form><form onSubmit={run} className="rounded-md border border-line bg-white p-5"><h2 className="font-semibold">导入文章候选</h2><div className="mt-4 grid gap-3"><select required value={ingest.sourceId} onChange={(e) => setIngest({ ...ingest, sourceId: e.target.value })} className={input}><option value="">选择已审核来源</option>{sources.map((item) => <option key={String(item.id)} value={String(item.id)}>{String(item.name)}</option>)}</select><input required type="url" value={ingest.url} onChange={(e) => setIngest({ ...ingest, url: e.target.value })} placeholder="同一官方域名下的文章 URL" className={input} /><select value={ingest.category} onChange={(e) => setIngest({ ...ingest, category: e.target.value })} className={input}><option value="activity">活动</option><option value="health_classroom">家医小课堂</option><option value="notice">通知</option><option value="schedule_notice">排班通知</option><option value="policy">政策</option></select><button className="rounded-md bg-navy px-4 py-3 text-sm font-semibold text-white">安全采集并送审</button></div></form></div><section className="mt-6 rounded-md border border-line bg-white p-5"><div className="flex items-center justify-between"><h2 className="font-semibold">来源台账</h2><Link href="/workbench/operations" className="text-sm font-semibold text-sage">待审核 {candidateCount} 条</Link></div><div className="mt-4 divide-y divide-line">{sources.map((item) => <div key={String(item.id)} className="flex items-center gap-3 py-3"><div className="min-w-0 flex-1"><p className="text-sm font-semibold">{String(item.name)}</p><p className="mt-1 truncate text-xs text-navy/45">{String(item.source_url)}</p></div><a href={String(item.source_url)} target="_blank" rel="noreferrer" aria-label="打开来源"><ExternalLink className="h-4 w-4 text-sage" /></a></div>)}</div></section></div></main>; }
